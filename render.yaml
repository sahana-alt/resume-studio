services:
  - type: web
    name: resume-studio
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: HOST=0.0.0.0 python3 src/app_server.py
    envVars:
      - key: APP_USERNAME
        value: sahana
      - key: APP_PASSWORD
        sync: false
